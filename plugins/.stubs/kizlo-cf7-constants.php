<?php

/**
 * PHPStan stubs for the kizlo-cf7 plugin's bootstrap constants.
 *
 * These are define()'d in the main kizlo-cf7.php with dynamic values
 * (plugin_dir_path(__FILE__) etc.), which PHPStan can't pick up from a scanned
 * file — so they're declared here as plain consts purely for symbol/type
 * resolution. Values are placeholders; only the type (string) matters.
 * Stub only — never loaded at runtime, never shipped.
 */

const KIZLO_CF7_VERSION = '';
const KIZLO_CF7_FILE = '';
const KIZLO_CF7_PATH = '';
const KIZLO_CF7_URL = '';
const KIZLO_CF7_BASENAME = '';