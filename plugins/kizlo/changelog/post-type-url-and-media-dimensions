Significance: minor
Type: added

Post-type API responses now include the resolved public `url` (built from the Kizlo site-URL setting) and a full `featured_media` object with `width`, `height`, `mime`, responsive `variants`, and a ready-to-use `srcset` string (generated by WordPress' own resolver) via the shared media resolver.
