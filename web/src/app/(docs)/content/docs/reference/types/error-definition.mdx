---
title: ErrorDefinition
description: The shape of a single error entry in an error map.
types: [ErrorDefinition]
---

An `ErrorDefinition` describes one error code in a map passed to
[`defineErrorMap`](/docs/reference/functions/define-error-map). Every field is optional, so a code can be declared
with nothing but its name.

<AutoTypeTable
  path="../packages/kizlo/src/shared/error.ts"
  name="ErrorDefinition"
  type={`ErrorDefinition`}
/>
