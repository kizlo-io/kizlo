---
"kizlo": minor
---

Give a template's `bootstrap.command` a free-form command string with `{{pm}}`, `{{name}}`, and `{{dlx}}` tokens, letting templates use any scaffolding invocation instead of a fixed `<pm> create <initializer> <name>` shape
