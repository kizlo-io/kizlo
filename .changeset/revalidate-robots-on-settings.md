---
"kizlo": patch
---

Revalidate the global SEO surfaces on `settings.saved` events in the Next.js revalidation extension. Settings changes now refresh `/robots.txt` and the sitemap (both derived from settings) instead of being dropped.
