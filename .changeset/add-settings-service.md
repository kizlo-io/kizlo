---
"kizlo": minor
---

Add `service.settings`, a typed client for reading and updating Kizlo settings — `settings.get()`, `settings.updateSite(...)`, `settings.updateWebhook(...)`, and the other per-section update methods.