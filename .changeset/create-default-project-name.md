---
"kizlo": minor
---

Make the `create` project name optional: press enter to accept a generated placeholder name, enter `.` to scaffold into the current directory, and show a bare folder as `./name` so it's clear where it lands
