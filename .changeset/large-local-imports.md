---
"kizlo": patch
---

Support large uploads and full-site imports on local WordPress stacks by raising the PHP upload limit to 2 GB and making same-site server requests reachable inside the container.
