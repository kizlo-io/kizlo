---
"kizlo": patch
---

Require an explicit `enable: true` to turn on each local dev or test stack, and run `kizlo dev` and `kizlo generate` with introspection skipped instead of failing when no WordPress connection is reachable.
