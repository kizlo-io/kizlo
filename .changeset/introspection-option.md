---
"kizlo": minor
---

Replace the `wordpress` option on `createKizlo` with `introspection`; pass the `introspection` export of your generated barrel and let integrations supply WordPress credentials.
