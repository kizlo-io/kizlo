---
"kizlo": minor
---

Add template-defined prompts that map create-time choices to framework CLI flags, so a template can surface a real framework option (e.g. which linter) instead of hard-coding it in `bootstrap`
