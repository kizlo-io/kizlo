{
	"name": "@templates/astro",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev:template": "pnpm withEnv -- astro dev",
		"build": "pnpm withEnv -- astro build",
		"preview": "pnpm withEnv -- astro preview",
		"typecheck": "astro check",
		"clean": "git clean -xdf .cache .turbo .kizlo .astro dist node_modules",
		"withEnv": "dotenv -e ../../.env"
	},
	"dependencies": {
		"@astrojs/node": "catalog:",
		"astro": "catalog:",
		"kizlo": "workspace:*"
	},
	"devDependencies": {
		"@astrojs/check": "^0.9.9",
		"@tooling/typescript": "workspace:*",
		"dotenv-cli": "catalog:",
		"typescript": "catalog:"
	}
}
